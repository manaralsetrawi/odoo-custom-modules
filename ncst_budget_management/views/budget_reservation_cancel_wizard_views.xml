<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_budget_reservation_cancel_wizard_form" model="ir.ui.view">
        <field name="name">budget.reservation.cancel.wizard.form</field>
        <field name="model">budget.reservation.cancel.wizard</field>
        <field name="arch" type="xml">
            <form string="Cancel Reservation">
                <sheet>
                    <group>
                        <field name="budget_reservation_id" readonly="1"/>
                        <field name="cancel_reason" placeholder="Enter the cancellation reason..."/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_confirm_cancel"
                            string="Confirm Cancel"
                            type="object"
                            class="btn-danger"/>
                    <button string="Close"
                            special="cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>