<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view -->
    <record id="view_budget_reservation_tree" model="ir.ui.view">
        <field name="name">budget.reservation.tree</field>
        <field name="model">budget.reservation</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="date"/>
                <field name="general_budget_id"/>
                <field name="department_id"/>
                <field name="department_budget_id"/>
                <field name="request_ref"/>
                <field name="amount"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_budget_reservation_form" model="ir.ui.view">
        <field name="name">budget.reservation.form</field>
        <field name="model">budget.reservation</field>
        <field name="arch" type="xml">
            <form string="Budget Reservation">
                <header>
                    <button name="action_reserve"
                            string="Reserve"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_mark_used"
                            string="Mark as Used"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'reserved'"/>
                    <button name="action_cancel"
                            string="Cancel"
                            type="object"
                            invisible="state not in ['draft', 'reserved']"/>
                    <button name="action_reset_to_draft"
                            string="Reset to Draft"
                            type="object"
                            invisible="state not in ['reserved']"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,reserved,used,cancelled"/>
                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="department_budget_id"/>
                            <field name="department_id" readonly="1"/>
                            <field name="general_budget_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="amount"/>
                            <field name="currency_id" readonly="1"/>
                            <field name="date"/>
                            <field name="request_ref"/>
                        </group>
                    </group>

                    <group>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_budget_reservation" model="ir.actions.act_window">
        <field name="name">Budget Reservations</field>
        <field name="res_model">budget.reservation</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>